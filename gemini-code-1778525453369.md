# SYSTEM DIRECTIVE: Cloud Computing Dashboard Refactor

## 1. Project Context
* **Current Branch:** `feature/cloud-computing-project`
* **Target File:** `app.py`
* **Objective:** Refactor the Streamlit dashboard to be highly professional, integrate dynamic node management, and add Particle Swarm Optimization (PSO). The user explicitly requested the removal of all emojis and dramatic "AI" phrasing in favor of short, specific DevOps terminology.

## 2. Core Tasks

### Task A: Professional UI Refactor (Remove Fluff)
* Remove ALL emojis (☁️, 🚀, 🧠, ⚙️, 🖥️, 📊, 📁, 🔍, ⚡, etc.) from `app.py`.
* Rename verbose titles and buttons. For example:
  * "AWS Cloud Task Scheduling Console" -> "Cloud Task Scheduler"
  * "Deploy Cloud Simulation" -> "Run Simulation"
  * "CI AI Makespan" -> "Makespan"
  * "CI AI Total Cost" -> "Total Cost"
  * "Active EC2 Nodes" -> "Cluster Nodes"
* Keep the dark mode/terminal CSS styling, but ensure the language is concise and technical.

### Task B: Dynamic Node Management
Currently, `generate_nodes()` in `src/data_loader.py` creates static nodes. We must bypass this in `app.py` by managing the node cluster in Streamlit's `session_state`.
1. Initialize `st.session_state['custom_nodes']` with 5 default AWS instances if it doesn't exist:
   * Node 1: speed=1.0 CU, cost=$0.0116 (t2.micro)
   * Node 2: speed=2.0 CU, cost=$0.0208 (t3.small)
   * Node 3: speed=4.0 CU, cost=$0.0340 (c6g.medium)
   * Node 4: speed=8.0 CU, cost=$0.0960 (m5.large)
   * Node 5: speed=16.0 CU, cost=$0.1260 (r5.large)
2. Add a UI section in the sidebar (or main config area) that displays the current nodes.
3. Add form inputs to allow the user to "Add Node" (Input: Speed CU, Cost/Hr) and a way to "Clear/Reset Nodes".
4. Ensure the `run_simulation` function uses `st.session_state['custom_nodes']` instead of the randomly generated nodes.

### Task C: Integrate Particle Swarm Optimization (PSO)
* Ensure `PSO` is imported from `src.optimizers`.
* Add "Particle Swarm (PSO)" to the `scheduler_options` array.
* In the execution loop, if "Particle Swarm (PSO)" is selected, initialize and run `PSO(jobs, nodes, swarm_size=pop_size, max_iter=generations)`.

## 3. Constraints
* Provide the fully refactored `app.py` code.
* Ensure the UI does not crash when nodes are added or removed (handle state updates cleanly).
* Maintain the `pandas` DataFrame usage for the Node Assignment Inspector to avoid object rendering bugs.